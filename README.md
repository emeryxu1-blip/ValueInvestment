# Value Investment

The application source, setup guide, Cloudflare deployment instructions, and
architecture documentation are in [`site/`](site/README.md).

```bash
cd site
npm install
npm run db:migrate:local
npm run dev
```
